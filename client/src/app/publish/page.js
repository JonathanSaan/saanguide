import PublishPage from "../components/publishPage";

export const metadata = {
  title: "Publish - saanguide",
  description: "Publication Creation.",
};

const Publish = () => {
  return <PublishPage />;
};

export default Publish;
